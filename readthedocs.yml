# readthedocs.yml
version: 2

requirements_file: docs/requirements.txt


# Build configuration
build:
  os: ubuntu-22.04
  tools:
    python: "3.8"

# Setup submodules
submodules:
  include: all

# Configuration spécifique à Sphinx
sphinx:
  configuration: docs/conf.py
